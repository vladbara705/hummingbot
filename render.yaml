services:
  - name: hummingbot
    type: web
    env: docker
    dockerfilePath: ./Dockerfile
    image:
      url: hummingbot/hummingbot:latest
    runtime: docker
    disk:
      name: ./
      mountPath: /home/hummingbot
